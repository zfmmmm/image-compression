"""Traditional image codec wrappers.

The package directory is intentionally named ``codecs`` to match the project
specification.  Runtime import compatibility is handled by ``sitecustomize.py``
and ``utils.codec_imports`` because Python already has a standard-library
module with the same name.
"""
